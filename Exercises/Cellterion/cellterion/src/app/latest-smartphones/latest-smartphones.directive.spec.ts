import { LatestSmartphonesDirective } from './latest-smartphones.directive';

describe('LatestSmartphonesDirective', () => {
  it('should create an instance', () => {
    const directive = new LatestSmartphonesDirective();
    expect(directive).toBeTruthy();
  });
});
